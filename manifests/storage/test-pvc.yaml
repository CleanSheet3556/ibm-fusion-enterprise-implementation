apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fusion-demo-pvc
  namespace: fusion-demo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # Set this explicitly after discovering the approved StorageClass.
  # storageClassName: <approved-storage-class>
