# Backup Architecture

See the corresponding detailed design under `docs/`. The companion Mermaid diagram under `architecture/diagrams/` is the visual reference. Release-specific product behavior must be verified against IBM/Red Hat documentation before implementation.
