# OpenShift Virtualization host/provider

The provider concept in this project represents the OpenShift Virtualization environment that hosts the VM workload. IBM Fusion migration/management workflows may expose provider concepts through their management experience.

Validate the provider from the actual Fusion release UI and capture the relevant resource/health state.

Do not infer a provider object name from another product/version.
